# Telegram-бот для заявок + веб-админка на Laravel

Готовое решение для приёма заявок через Telegram: клиент выбирает услугу в боте
или собирает корзину в мини-приложении (Telegram Mini App) и оставляет заявку за минуту, оплачивает её картой прямо в чате, администратор
видит заявку в веб-панели, меняет статус, отвечает клиенту прямо из админки
и запускает рассылки по базе пользователей. Страница аналитики показывает,
сколько людей приходит в бота, где они отсеиваются и что покупают.

В одной системе работает несколько ботов (например, филиалы или бренды) с общим
каталогом, а бот и мини-приложение говорят с клиентом по-русски или по-английски.

## Как посмотреть

**Живой демо-бот:** [@LaravelsBot](https://t.me/LaravelsBot) — нажмите `/start`,
оформите заявку в чате или откройте каталог и корзину кнопкой меню слева
от поля ввода (Mini App). Бот развёрнут на виртуальном хостинге так, как описано
в разделе [Установка на виртуальный хостинг](#установка-на-виртуальный-хостинг-beget-и-аналоги).

Админку демо-стенда можно посмотреть по запросу. Чтобы изучить её самостоятельно,
поднимите проект локально одной командой:

```bash
cp .env.example .env
docker compose run --rm app php artisan key:generate --show   # впишите результат в APP_KEY
docker compose up -d
```

Админка откроется на <http://localhost:8080/admin>, учётные данные — из
`ADMIN_EMAIL` и `ADMIN_PASSWORD` в `.env` (по умолчанию `admin@example.com`
и `password`). Каталог услуг и учётка администратора создаются при первом старте.

Чтобы посмотреть админку не на пустых таблицах, добавьте демо-пользователей
и заявки:

```bash
docker compose exec app php artisan db:seed --class=DemoSeeder
```

Чтобы поговорить с ботом, нужен свой токен от [@BotFather](https://t.me/BotFather) —
см. [Настройка бота и webhook](#настройка-бота-и-webhook). Без токена админка
полностью работоспособна, не работает только отправка сообщений.

## Что умеет

**Бот**

- `/start` — приветствие и главное меню на инлайн-кнопках
- Каталог: категории → услуги → карточка услуги с ценой и описанием
- Оформление заявки в диалоге: имя → телефон (кнопкой «Отправить мой номер» или вручную) → комментарий → подтверждение
- Оплата картой прямо в Telegram (Telegram Payments + ЮKassa): кнопка «Оплатить» сразу после оформления и у каждой неоплаченной заявки
- `/orders` — список своих заявок со статусами и отметкой об оплате
- Уведомление клиенту при каждой смене статуса заявки
- `/help` — краткая инструкция
- `/language` — выбор языка: русский или английский; по умолчанию берётся язык Telegram

**Mini App (мини-приложение внутри Telegram)**

- Каталог с вкладками категорий и корзина: несколько услуг и количество в одной заявке
- Форма контактов с автозаполнением: имя из Telegram, телефон из прошлых заявок
- Нативные кнопки Telegram (MainButton, BackButton), виброотклик и цвета темы клиента: светлая и тёмная тема подхватываются сами
- Оплата заказа сразу в Mini App: счёт открывается поверх приложения, без перехода в чат
- Подтверждение с составом заявки дублируется в чат с ботом
- Интерфейс и каталог на языке клиента: тот же, что и в чате с ботом

**Админка**

- Несколько ботов: у каждого свои пользователи, заявки, переписка и рассылки, токены хранятся зашифрованными; проверка токена и установка webhook кнопкой
- Общий каталог с выбором, в каких ботах продаётся услуга, и переводом названий и описаний на английский
- Фильтр по боту в заявках, пользователях, рассылках и аналитике
- Дашборд: новые заявки, заявки за сегодня, число пользователей, выручка, последние заявки
- Заявки: вкладки по статусам, фильтры, поиск по номеру, клиенту и телефону
- Смена статуса одной кнопкой — клиент сразу получает сообщение в боте
- Ответ клиенту в Telegram прямо из админки, вся переписка сохраняется
- Оплаты: колонка и фильтр «Оплата» в списке заявок, история платежей в карточке заявки, счёт клиенту одной кнопкой, отметка возврата с уведомлением клиента
- Уведомление администраторам в Telegram о каждой оплате
- Аналитика за 7, 30 или 90 дней: новые и активные пользователи, заявки, конверсия, средний чек и оплаты со сравнением с прошлым периодом
- Графики новых пользователей и заявок по дням с разбивкой по каналам (бот или Mini App)
- Воронка: пришли в бота → открыли каталог → начали оформление → оставили заявку → оплатили
- Популярные услуги по сумме в заявках
- Канал заявки (бот или Mini App) в списке заявок: колонка и фильтр
- Состав заявки в карточке: позиции, цена, количество и сумма по каждой услуге
- Карточка пользователя: профиль, его заявки и история сообщений
- Каталог: категории и услуги с сортировкой перетаскиванием и флагом «показывать в боте»
- Рассылки: черновик → запуск через очередь → счётчики доставленных и ошибок в реальном времени
- Пользователи, заблокировавшие бота, помечаются автоматически и исключаются из рассылок

## Стек и почему он

| Технология | Версия | Почему |
|-----------|--------|--------|
| Laravel | 13 | Очереди, миграции, тесты из коробки |
| PHP | 8.4 | Enum-ы с методами, typed properties, named arguments |
| [Nutgram](https://nutgram.dev) | 1.x | Современный фреймворк для ботов: роутинг команд и callback-кнопок с параметрами, встроенные диалоги (Conversations) с сохранением шага, middleware, актуальная поддержка Bot API. Код бота получается компактным — достаточно взглянуть на `routes/telegram.php` |
| [Filament](https://filamentphp.com) | 5.x | Панель с CRUD, фильтрами, действиями и виджетами собирается за часы, а не недели. Построен на Livewire, поэтому кастомные экраны пишутся в той же парадигме |
| MySQL | 8.x | Стандарт для хостингов, где разворачивают такие проекты |
| Очередь | `database` | Работает без Redis на любом дешёвом хостинге. Драйвер меняется одной строкой в `.env` |

Бот работает через **webhook**, а не long polling: Telegram сам присылает
обновления на HTTPS-эндпоинт, сервер не держит постоянное соединение.
У каждого бота свой эндпоинт, поэтому один сервер обслуживает сколько угодно ботов.

## Установка

### Требования

- PHP 8.3+ с расширениями `intl`, `pdo_mysql`, `mbstring`, `curl`, `openssl`, `zip`
- Composer 2
- MySQL 8 или MariaDB 10.6+

### Шаги

```bash
git clone <repo> telegram-bot-admin
cd telegram-bot-admin

composer install
cp .env.example .env
php artisan key:generate
```

Создайте базу и пропишите доступы в `.env`:

```dotenv
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=telegram_bot_admin
DB_USERNAME=root
DB_PASSWORD=
```

Примените миграции и загрузите демо-каталог:

```bash
php artisan migrate --seed
```

Создайте администратора панели:

```bash
php artisan admin:create
```

Запустите приложение и воркер очереди (рассылки уходят через него):

```bash
php artisan serve
php artisan queue:work
```

Админка: <http://localhost:8000/admin>

### Запуск в OSPanel (Windows)

Положите проект в `C:\OSPanel\home\` — конфиг домена `.osp/project.ini` уже в репозитории,
он указывает Apache на `public` и подключает PHP 8.4 и MySQL 8.4. Перезапустите
Open Server Panel, чтобы она перечитала список проектов, и сайт откроется
на <http://02-telegram-bot-admin/admin> (HTTPS тоже работает — OSPanel сам выпускает
сертификат на домен).

Две детали, на которых легко споткнуться:

- Скорее всего `php` в PATH — это не PHP из OSPanel. Filament требует расширение `intl`,
  поэтому artisan и composer запускайте явно:
  `C:\OSPanel\modules\PHP-8.4\php.exe artisan ...`
- MySQL слушает не на `127.0.0.1`, а на собственном loopback-адресе модуля
  (например `127.0.1.28`). Точный IP показан в окне OSPanel и в `hosts`; его и пишем
  в `DB_HOST`. Либо используйте имя хоста, которое OSPanel прописывает сам: `mysql-8.4`.

### Запуск в Docker

Нужен только `.env` с заполненным `APP_KEY` — остальное поднимется само:

```bash
cp .env.example .env
docker compose run --rm app php artisan key:generate --show   # впишите результат в .env
docker compose up -d
```

Админка: <http://localhost:8080/admin>, вход из `ADMIN_EMAIL` и `ADMIN_PASSWORD`.

Стек состоит из трёх сервисов:

| Сервис | Роль |
|--------|------|
| `app` | FrankenPHP отдаёт `public/` напрямую, без связки nginx + php-fpm. На старте накатывает миграции и сидеры |
| `mysql` | MySQL 8.4, данные в именованном томе. Наружу отдан порт **3307** — 3306 обычно занят локальной базой |
| `queue` | `queue:work`, разбирает рассылки. Без него рассылка навсегда останется в статусе «В очереди» |

Все три роли — один и тот же образ, различаются только аргументом команды
(`serve`, `queue`, `bot`) — его разбирает [docker/entrypoint.sh](docker/entrypoint.sh).

Бот в Docker поднимается отдельным профилем, потому что снаружи webhook до
локального контейнера не достучится и остаётся long polling. Контейнер слушает
бота из `BOT_ID` в `.env` (по умолчанию — первый активный):

```bash
docker compose --profile bot up -d
```

Artisan-команды выполняются в работающем контейнере:

```bash
docker compose exec app php artisan telegram:webhook info
docker compose exec app php artisan admin:create
docker compose logs -f queue
```

Переменные `compose.yaml` берёт из `.env` проекта, поэтому токен бота в
репозиторий не попадает. `DB_HOST` внутри сети Docker всегда `mysql` — значение
из вашего `.env` перекрывается, локальная база OSPanel не задействована.

### Установка на виртуальный хостинг (Beget и аналоги)

Демо-бот работает на обычном виртуальном хостинге с SSH, без VPS и Docker.
Пример ниже — для Beget; у других хостингов отличаются только пути и названия
разделов панели.

**1. Панель хостинга.** Создайте базу MySQL, у сайта выберите PHP 8.4, включите SSH.

**2. Код и зависимости.** На Beget PHP вызывается как `php8.4`, а встроенный
Composer — версии 1, поэтому ставим Composer 2 в домашнюю папку:

```bash
mkdir -p ~/bin && cd ~/bin
php8.4 -r "copy('https://getcomposer.org/installer', 'composer-setup.php');"
php8.4 composer-setup.php --install-dir=$HOME/bin --filename=composer.phar && rm composer-setup.php

cd ~/site.example
git clone <repo> laravel && cd laravel
php8.4 ~/bin/composer.phar install --no-dev --optimize-autoloader
```

**3. Фронтенд.** На хостинге обычно нет Node.js: соберите фронт локально
(`npm run build`) и загрузите папку `public/build` на сервер.

**4. `.env`.** Скопируйте `.env.example`, выставьте `APP_ENV=production`,
`APP_DEBUG=false`, доступы к базе (`DB_HOST=localhost`), токен бота и
`TELEGRAM_ADMIN_CHAT_IDS`, затем:

```bash
php8.4 artisan key:generate
php8.4 artisan migrate --seed --force
php8.4 artisan storage:link
php8.4 artisan optimize
```

**5. Корень сайта.** Сайт должен смотреть в `public/`. Если в панели нельзя
сменить корень, замените папку сайта символической ссылкой:

```bash
cd ~/site.example && mv public_html public_html.bak && ln -s laravel/public public_html
```

**6. Очередь.** Постоянный воркер на виртуальном хостинге держать нельзя, поэтому
в панели в разделе «CRON» добавьте задачу на каждую минуту. Планировщик сам
запустит `queue:work`, который разберёт рассылки и завершится:

```bash
/usr/local/bin/php8.4 /home/<путь-к-проекту>/artisan schedule:run >> /dev/null 2>&1
```

**7. HTTPS.** Webhook и Mini App работают только по HTTPS. Если на сайт можно
выпустить бесплатный сертификат Let's Encrypt — пропишите `https://` адрес в `APP_URL`.
На технический адрес хостинга (например, `*.beget.tech`) сертификат не выпускается —
тогда бесплатный HTTPS даёт [Cloudflare Worker](https://workers.cloudflare.com)
с адресом `*.workers.dev`, который пересылает запросы на сайт по HTTP:

```js
export default {
  async fetch(request) {
    const url = new URL(request.url);
    url.protocol = 'http:';
    url.hostname = 'site.example';
    url.port = '';

    return fetch(new Request(url, request), { redirect: 'manual' });
  },
};
```

Адрес Worker прописывается в `APP_URL` и `TELEGRAM_MINI_APP_URL`. Если `APP_URL`
начинается с `https://`, приложение строит все ссылки от него, поэтому админка
и Mini App за прокси работают без доверия к заголовкам `X-Forwarded-*`.

**8. Бот.** После `php8.4 artisan config:cache` установите webhook, меню команд
и кнопку Mini App:

```bash
php8.4 artisan telegram:webhook set
php8.4 artisan telegram:commands
php8.4 artisan telegram:menu-button set
php8.4 artisan telegram:webhook info    # «Последняя ошибка» должна быть пустой
```

Обновление кода на сервере: `git pull`, при изменении зависимостей —
`composer install --no-dev`, затем `php8.4 artisan migrate --force && php8.4 artisan optimize`.
Фронтенд после правок снова собирается локально и загружается в `public/build`.

## Настройка бота и webhook

Ботов может быть несколько: каждый заводится в админке, в разделе **Настройки → Боты**.
Токены хранятся в базе в зашифрованном виде (ключ — `APP_KEY`), в код и в репозиторий
они не попадают.

### 1. Создать бота

Напишите [@BotFather](https://t.me/BotFather) команду `/newbot` и получите токен.
Дальше два пути:

- **через админку:** Настройки → Боты → «Создать», вставить токен и нажать
  в списке «Проверить токен» — username подтянется из Telegram;
- **при первой установке:** впишите токен в `.env` — `php artisan db:seed` сам заведёт
  из него первого бота:

  ```dotenv
  TELEGRAM_TOKEN=123456789:AAH...            # токен, который выдал BotFather
  TELEGRAM_BOT_USERNAME=имя_вашего_бота      # без @
  ```

Новый бот сразу продаёт весь активный каталог. Если в каком-то боте услуга не нужна,
снимите галочку в карточке услуги, в блоке «Боты».

### 2. Указать chat_id администраторов

Напишите боту [@userinfobot](https://t.me/userinfobot), чтобы узнать свой `chat_id`,
и перечислите получателей уведомлений через запятую:

```dotenv
TELEGRAM_ADMIN_CHAT_IDS=123456789,987654321
```

Уведомление приходит от того бота, в котором оформили заявку. Telegram не даёт боту
написать первым, поэтому администратору нужно один раз нажать `/start` в каждом боте.

### 3. Установить webhook

У каждого бота свой адрес `APP_URL/telegram/webhook/{id}` и свой секрет. Секрет
генерируется при создании бота; Telegram присылает его в заголовке
`X-Telegram-Bot-Api-Secret-Token`, и `VerifyTelegramWebhook` отклоняет всё остальное —
в том числе запрос с секретом другого бота.

Telegram требует HTTPS, поэтому локально нужен туннель:

```bash
cloudflared tunnel --url http://localhost:8000     # или: ngrok http 8000
```

Пропишите выданный адрес в `APP_URL` и установите webhook — кнопкой «Установить webhook»
в списке ботов или командой:

```bash
php artisan telegram:webhook set                  # все активные боты
php artisan telegram:webhook set --bot=2          # один бот
php artisan telegram:webhook info                 # статус и последняя ошибка
php artisan telegram:webhook remove
```

Без туннеля бота можно запустить в режиме long polling — один процесс на бота:

```bash
php artisan telegram:poll        # первый активный бот
php artisan telegram:poll 2      # бот с id 2
```

### 4. Зарегистрировать меню команд

```bash
php artisan telegram:commands
```

Команды `/start`, `/help`, `/orders`, `/language` появятся у поля ввода — на русском
и на английском, Telegram сам покажет вариант под язык клиента.

### 5. Подключить оплату (необязательно)

Токен платёжного провайдера BotFather выдаёт на конкретного бота, поэтому он
хранится в карточке бота:

1. В [@BotFather](https://t.me/BotFather): `/mybots` → бот → **Payments** → **ЮKassa**.
   Для проверки выберите тестовый режим: BotFather выдаст тестовый токен провайдера.
2. Вставьте его в админке: Боты → бот → «Токен платёжного провайдера».

Пока токен пустой, кнопки оплаты в этом боте и в заявках его клиентов скрыты.
Для боевых платежей нужен магазин в ЮKassa и боевой токен из того же меню BotFather.

Если в ЮKassa подключена онлайн-касса, включите чеки по 54-ФЗ:
`TELEGRAM_PAYMENT_RECEIPT=true` и код ставки НДС в `TELEGRAM_PAYMENT_VAT_CODE`
(1 — без НДС). Бот начнёт спрашивать телефон в форме оплаты и передавать его
в ЮKassa вместе с позициями чека. Чек всегда на русском, даже если клиент
общается с ботом по-английски.

Оплата работает и через webhook, и через long polling: отдельный эндпоинт для
уведомлений от ЮKassa не нужен, всё приходит апдейтами Telegram.

### 6. Подключить Mini App (необязательно)

Telegram открывает Mini App только по **https**. У каждого бота своя страница
`/app/{id}` с его каталогом.

1. Соберите фронт: `npm install && npm run build`.
2. Впишите https-адрес сайта в `.env`:

   ```dotenv
   TELEGRAM_MINI_APP_URL=https://ваш-домен
   ```

3. Поставьте Mini App на кнопку меню слева от поля ввода:

   ```bash
   php artisan telegram:menu-button set     # reset — вернуть список команд
   ```

В главном меню бота появится кнопка «🛒 Каталог и корзина». Пока адрес не задан
или начинается с `http://`, кнопка скрыта и бот работает как раньше.

### 7. Языки

Бот и Mini App говорят по-русски и по-английски. Язык клиента выбирается так:

1. выбор через `/language` или кнопку «🌐 Язык / Language» в меню;
2. иначе — язык приложения Telegram, если он поддерживается;
3. иначе — язык бота по умолчанию из его карточки в админке.

Тексты лежат в `lang/ru` и `lang/en` (`bot.php` — бот, `mini-app.php` — Mini App).
Названия и описания услуг и категорий переводятся в админке, в блоке «Перевод»
формы. Без перевода клиент увидит русский текст. Админка и уведомления
администраторам остаются на русском.

### Обновление с одного бота на несколько

Миграция сама создаст первого бота из прежних `TELEGRAM_TOKEN`,
`TELEGRAM_WEBHOOK_SECRET` и `TELEGRAM_PAYMENT_PROVIDER_TOKEN` и отдаст ему всех
пользователей, заявки и рассылки. Адрес webhook изменился
(`/telegram/webhook` → `/telegram/webhook/{id}`), поэтому после обновления
выполните `php artisan telegram:webhook set`.

## Команды проекта

| Команда | Что делает |
|---------|-----------|
| `php artisan admin:create` | Создать администратора веб-панели |
| `php artisan telegram:webhook set\|info\|remove [--bot=ID]` | Управление webhook ботов |
| `php artisan telegram:poll [ID]` | Запустить бота в режиме long polling |
| `php artisan telegram:commands [--bot=ID]` | Меню команд ботов на всех языках |
| `php artisan telegram:menu-button set\|reset [--bot=ID]` | Открывать Mini App кнопкой меню бота |
| `php artisan nutgram:list` | Показать все зарегистрированные обработчики |
| `php artisan queue:work` | Воркер очереди: рассылки |
| `php artisan db:seed --class=DemoSeeder` | Наполнить админку демо-ботами, пользователями, заявками, оплатами и историей за 30 дней |
| `php artisan test` | Прогнать тесты |
| `vendor/bin/pint` | Привести код к стилю Laravel |

## Структура

```
app/
├── Console/Commands/        admin:create, telegram:webhook, telegram:poll, telegram:commands, telegram:menu-button
├── Enums/                   OrderStatus, OrderSource, PaymentStatus, BroadcastStatus, MessageDirection, BotEventType
├── Filament/
│   ├── Actions/             Смена статуса, ответ клиенту, счёт на оплату, запуск рассылки
│   ├── Pages/               Analytics — страница аналитики с фильтром периода
│   ├── Resources/           Заявки, пользователи, услуги, категории, рассылки, боты
│   └── Widgets/             Дашборд; Analytics\ — цифры, графики, воронка, популярные услуги
├── Http/
│   ├── Controllers/         TelegramWebhookController, MiniApp\ — страница, профиль, заявка
│   ├── Middleware/          VerifyTelegramWebhook, AuthenticateMiniApp
│   └── Requests/            MiniApp\StoreOrderRequest
├── Jobs/                    StartBroadcast, SendBroadcastMessage
├── Models/                  Bot, TelegramUser, Service, ServiceCategory, Order, OrderItem, Payment, Broadcast, BotMessage, BotEvent
├── Observers/               OrderObserver — уведомление клиента о смене статуса
├── Services/                OrderService, PaymentService, BroadcastService, Analytics\AnalyticsService,
│                            Telegram\BotMessenger, Telegram\InitDataValidator
└── Telegram/
    ├── BotManager.php       Экземпляр Nutgram на каждого бота из таблицы bots
    ├── Commands/            /start
    ├── Conversations/       OrderConversation — диалог оформления заявки
    ├── Handlers/            Меню, каталог, мои заявки, оплата
    ├── Middleware/          TrackTelegramUser — регистрация и лог входящих
    └── Support/             Keyboards, Texts, Screen, BotContext

lang/
├── ru/                      bot.php — тексты бота, mini-app.php — тексты Mini App
└── en/                      то же на английском

resources/
├── views/mini-app.blade.php  Разметка Mini App
└── js/mini-app.js           Каталог, корзина и оформление на Alpine.js

docker/
└── entrypoint.sh            Выбор роли контейнера: serve, queue, bot

routes/
├── telegram.php             Обработчики бота
└── web.php                  Webhook, Mini App и его API, редирект на админку
```

### Как устроен обмен сообщениями

Все исходящие сообщения проходят через `Telegram\BotMessenger`. Это бесплатно даёт
три вещи: переписка пишется в `bot_messages` и видна в карточке клиента, ответ
`403 Forbidden` помечает пользователя как заблокировавшего бота, а удачная
доставка эту отметку снимает.

Уведомление о смене статуса отправляет `OrderObserver`, а не кнопка в админке.
Поэтому сообщение уходит клиенту и когда статус меняют кнопкой, и когда правят
через форму редактирования, и когда меняют кодом.

Название и цена услуги копируются в заявку в момент оформления. Поменяли прайс
или убрали услугу из каталога — старые заявки остаются такими, какими их видел клиент.

### Как устроена оплата

Используется Telegram Payments: счёт приходит клиенту сообщением, карта вводится
в интерфейсе Telegram, деньги списывает провайдер (ЮKassa). Вся логика — в `PaymentService`:

1. **Счёт.** На заявку заводится платёж в статусе «ожидает оплаты» со случайным
   `invoice_payload`. Повторный счёт по той же заявке использует тот же платёж
   и подтягивает актуальную цену.
2. **Pre-checkout.** Перед списанием Telegram спрашивает бота, можно ли принять
   оплату, и ждёт ответ не дольше 10 секунд. Бот отказывает, если заявку отменили,
   она уже оплачена или цена изменилась после выставления счёта, — клиент видит
   причину, деньги не списываются.
3. **Списание.** Приходит `successful_payment`. Платёж и заявка отмечаются
   оплаченными, клиент и администраторы получают сообщения. Повторный апдейт
   с тем же `telegram_payment_charge_id` игнорируется, а второе списание по одному
   счёту сохраняется отдельным платежом и помечается для возврата.

Если оплата пришла, пока клиент оформляет новую заявку, обработчик оплаты
прерывает диалог: иначе апдейт попал бы в шаг диалога и платёж не был бы учтён.

Вернуть деньги за карточный платёж через Bot API нельзя, поэтому возврат
делается в личном кабинете ЮKassa, а в админке фиксируется кнопкой
«Отметить возврат» — клиенту уходит уведомление.

### Как устроен Mini App

Mini App — обычная страница `/app/{id бота}` на Blade, Alpine.js и Tailwind, которую Telegram
открывает во встроенном браузере. Каталог отдаётся сразу в HTML, корзина хранится
в `localStorage`, а оформление идёт через JSON API `/app/{id бота}/api/*`.

**Кто прислал запрос.** Страницу может открыть кто угодно, поэтому API доверяет
только `Telegram.WebApp.initData`: это строка с данными пользователя, которую
Telegram подписывает токеном бота. Фронт шлёт её в заголовке `X-Telegram-Init-Data`,
`AuthenticateMiniApp` проверяет подпись (HMAC-SHA256 по алгоритму из документации
Telegram) и срок действия (`TELEGRAM_MINI_APP_AUTH_TTL`). Подделать пользователя
или переиграть старую строку не выйдет — запрос получит `401`.

**Цены.** С фронта приходят только id услуг и количество. Цены и итог считает
сервер по каталогу, услуги из скрытых категорий или снятые с продажи отклоняются.

**Заявка с позициями.** Корзина превращается в одну заявку: состав лежит
в `order_items`, в самой заявке — итог и сводка вида «Лендинг и ещё 2». Заявки
из диалога в боте устроены так же, только позиция одна. Заявки, созданные
до появления корзины, миграция перенесла в позиции автоматически.

**Оплата из Mini App.** Вместо сообщения со счётом сервер создаёт ссылку
(`createInvoiceLink`), и фронт открывает её через `openInvoice` поверх приложения.
Pre-checkout и `successful_payment` приходят боту как обычно — логика оплаты общая.
Счёт и чек разбиты по позициям; если администратор вручную поменял цену заявки,
счёт идёт одной строкой, чтобы сумма позиций совпала с суммой к оплате.

### Как устроена аналитика

Большую часть цифр дают уже существующие таблицы: пользователи, заявки, платежи.
Отдельно пишутся только два шага, которых там не видно, — в таблицу `bot_events`:

- **открыл каталог** — кнопка «Каталог услуг» в боте или запуск Mini App;
- **начал оформление** — «Оставить заявку» в боте или переход в корзину Mini App.

Mini App сообщает о шагах через `POST /app/{id бота}/api/events` под той же проверкой `initData`,
что и заявки. Ответа не ждёт: сбой статистики не мешает оформлению.

Воронка строится по когорте — пользователям, пришедшим в бота за выбранный период.
Шаг засчитывается, если человек дошёл до него **или дальше**. Поэтому заявки,
оформленные до появления событий, не ломают воронку: следующий шаг никогда
не бывает больше предыдущего.

У заявки есть канал — `bot` или `mini_app`, — по нему строится график заявок
и фильтр в списке. Все расчёты собраны в `AnalyticsService`, виджеты только рисуют.

### Как устроены несколько ботов

Пакет Nutgram рассчитан на одного бота с токеном из `.env`. Здесь боты лежат в таблице
`bots`, а `App\Telegram\BotManager` собирает для каждого свой экземпляр Nutgram
с его токеном и теми же обработчиками из `routes/telegram.php`.

- **Входящие.** Webhook у каждого бота свой: `/telegram/webhook/{id}`. По адресу
  понятно, чей это апдейт; обработчики узнают своего бота через `BotContext::bot()`.
- **Исходящие.** `BotMessenger` отправляет сообщение от бота, в котором живёт
  пользователь. Ответ из админки, рассылка или статус заявки не придут клиенту
  от чужого бота.
- **Данные.** Пользователь уникален в паре «бот + chat_id»: один человек в двух ботах —
  две записи со своей перепиской, языком и отметкой о блокировке. Заявки и рассылки
  хранят `bot_id`, поэтому фильтры и аналитика по боту работают без join.
- **Каталог.** Он общий, а таблица `bot_service` решает, в каких ботах продаётся
  услуга. Бот, Mini App и оформление заявки проверяют это одинаково — через scope
  `Service::orderable($bot)`.
- **Mini App.** `initData` проверяется токеном бота из адреса: данные, подписанные
  одним ботом, не откроют API другого.

### Как устроены языки

`TrackTelegramUser` на каждом апдейте ставит язык приложения по пользователю,
поэтому обработчики просто вызывают `__()`. Там, где сообщение уходит не в ответ
на апдейт — смена статуса из админки, возврат, подтверждение из Mini App, —
текст собирается через `Texts::for($user, ...)`: на время сборки включается язык
клиента, потом возвращается прежний.

Переводы каталога хранятся в JSON-колонке `translations` рядом с русскими `name`
и `description`. Метод `translated()` отдаёт перевод или русский текст.
В заявке фиксируется русское название: так её видит администратор и так
оно попадает в чек по 54-ФЗ.

Mini App получает словари и названия услуг сразу на всех языках: при открытии
страницы сервер ещё не знает пользователя, потому что `initData` не приходит
в запросе страницы. Язык выбирается на фронте — сначала по Telegram, затем по
профилю с сервера, где учтён выбор через `/language`.

## Тесты

```bash
php artisan test
```

Покрыты:

- команды бота `/start`, `/help`, `/orders` и регистрация пользователя
- каталог: скрытие неактивных категорий и услуг, пустой каталог
- полный сценарий заявки: имя → телефон → комментарий → подтверждение
- валидация телефона и отмена заявки на любом шаге
- фиксация названия и цены услуги в заявке
- webhook: отказ без секрета, с неверным секретом и без настроенного секрета
- уведомление клиента при смене статуса, в том числе при правке через форму
- пометка заблокировавших бота по ответу Telegram
- рассылка: постановка в очередь, исключение заблокированных, задача на получателя, счётчики
- оплата: счёт по кнопке и из админки, запрет оплаты чужой заявки, данные чека 54-ФЗ
- pre-checkout: отказ для отменённой, уже оплаченной заявки, устаревшей суммы и неизвестного счёта
- успешная оплата: отметка заявки, уведомления, идемпотентность, повторное списание, оплата во время диалога
- возврат: отметка в админке, уведомление клиента, заявка с несколькими платежами
- доступность всех экранов админки и карточек
- Mini App: проверка `initData` — без подписи, с подменённым пользователем, чужим токеном и просроченной датой
- Mini App: заявка из корзины с ценами из каталога, позиции, отказ для недоступной услуги, валидация, уведомления
- Mini App: ссылка на счёт с разбивкой по позициям и одна строка после ручной правки цены
- кнопка Mini App в меню бота: показ для https-адреса и скрытие для http
- аналитика: воронка «дошёл до шага или дальше», исключение отменённых заявок и возвратов, разбивка по каналам с пустыми днями, популярные услуги, сравнение периодов
- запись шагов воронки в боте и Mini App, отказ для неизвестного события и без `initData`
- виджеты аналитики и то, что они не попадают на основной дашборд
- несколько ботов: шифрование токенов, отправка от бота пользователя, каталог и заявки в пределах бота, чужой `initData`, рассылка только подписчикам своего бота, фильтр аналитики по боту
- webhook: секрет другого бота, выключенный бот, один человек в двух ботах
- команды `telegram:webhook` и `telegram:commands` для каждого активного бота и языка
- языки: английский клиент, выбор через `/language`, язык бота по умолчанию, перевод каталога с откатом на русский, статус из админки на языке клиента, уведомление админу на русском
- раздел «Боты» в админке: создание, обязательный токен, токен не затирается пустым полем и не выводится на страницу
- ссылки от https-адреса `APP_URL`, когда сайт открыт через прокси

Тесты не ходят в Telegram: пакет подменяет клиент на `Nutgram::fake()`,
исходящие запросы проверяются ассертами.

## Лицензия

MIT
